// creating a function that will add two numbers and also subtract two numbers and return both as a list
function calculateNumbers(num1, num2) {
    let sum = num1 + num2;
    let difference = num1 - num2;
    return [sum, difference];
} 